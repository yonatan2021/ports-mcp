# Port Manager — Environment Configuration
# Copy this file to .env and adjust as needed.

# Port the HTTP UI listens on (default: 9999)
# PORT=9999

# Host interface (default: 127.0.0.1 — localhost only)
# Keep this local-only unless you add external authentication.
# HOST=127.0.0.1
